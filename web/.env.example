# Copy to .env.local for local dev (never commit the real .env.local).
# Base URL of the TwinArchitect API (Phase 5). No hardcoded API URL in
# component code — this is the one place it's configured.
VITE_API_URL=http://localhost:3001
